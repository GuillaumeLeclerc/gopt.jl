module Shufflers

function Independent(popSize)

    function shuffle()
    end
    (
     popSize=popSize,
     stateType=nothing,
     init=nothing,
     shuffle=shuffle,
     callEvery=0
    )
end

end
